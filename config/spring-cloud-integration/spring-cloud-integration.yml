spring:
  application:
    name: spring-cloud-integration
  profiles:
    active: local
server:
  port: 8081

# Application specific properties
app:
  message: "Hello from Spring Cloud Integration local!"

logging:
  level:
    com.example: DEBUG
    io.getunleash: DEBUG